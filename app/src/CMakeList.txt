cmake_minimum_required(VERSION 3.18.1)
project("celestik_native")

add_library(native-lib SHARED src/main/cpp/native-lib.cpp)

# Aquí debes ajustar la ruta a OpenCV según donde tengas el SDK de OpenCV para Android
set(OpenCV_DIR "C:/Users/compu/AndroidStudioProjects/celestik/opencv/sdk/native/jni")

find_package(OpenCV REQUIRED core)

find_library(log-lib log)

target_include_directories(native-lib PRIVATE ${OpenCV_INCLUDE_DIRS})

target_link_libraries(native-lib
                      ${log-lib}
                      ${OpenCV_LIBS})
